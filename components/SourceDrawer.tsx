"use client";
import { X, Mail, MessageCircle, Mic } from "lucide-react"; import type { Message } from "@/lib/types";
const icon={whatsapp:MessageCircle,email:Mail,meeting:Mic};
export function SourceDrawer({message,onClose}:{message:Message|null;onClose:()=>void}){if(!message)return null;const Icon=icon[message.source];return <><div className="drawer-backdrop" onClick={onClose}/><aside className="drawer"><div className="row" style={{justifyContent:"space-between",marginBottom:26}}><div><div className="project-label">Original source</div><h2 style={{margin:"5px 0 0"}}>Message evidence</h2></div><button className="button ghost" onClick={onClose}><X size={17}/></button></div><div className="row" style={{marginBottom:18}}><span className="source-icon"><Icon size={18}/></span><div><strong>{message.sender}</strong><div className="sub" style={{fontSize:13,textTransform:"capitalize"}}>{message.source} · {new Date(message.timestamp).toLocaleString()}</div></div></div>{message.label&&<div className="pill open" style={{display:"inline-block",marginBottom:15}}>{message.label}</div>}<div className="drawer-text">{message.content}</div><p className="sub" style={{fontSize:12,marginTop:20}}>This is the original imported text, shown unaltered for traceability.</p></aside></>}

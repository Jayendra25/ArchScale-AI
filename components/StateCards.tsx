"use client";
import type { Item, Message } from "@/lib/types";
export function StateCard({title,items,messages,onSource}:{title:string;items:Item[];messages:Message[];onSource:(m:Message)=>void}){return <section className="card"><h2>{title}</h2>{items.length?items.slice(0,4).map(x=>{const m=messages.find(m=>m.id===x.sourceMessageId);return <div className="item" key={x.id}><div className="item-title">{x.title}</div><div className="item-text">{x.description}</div><div className="meta"><span className={`pill ${x.status==='open'?'open':x.status}`}>{x.status.replace('_',' ')}</span>{x.owner&&<span className="sub" style={{fontSize:12}}>Owner: {x.owner}</span>}{m&&<button className="linkbutton" onClick={()=>onSource(m)}>View source</button>}</div></div>}):<p className="sub">Nothing to show yet.</p>}</section>}
